let places: string [] = [`new york`, `paris`, `dubai`, `karachi`];
console.log(`orignal `  + places);

console.log(`copy ` +[...places].sort());

console.log(`orignal `  + places);

console.log(`copy ` +[...places].sort().reverse());

console.log(`copy ` +[...places].sort().reverse());

console.log(`copy ` +[...places].sort());

console.log(`copy ` +[...places].sort().reverse());

console.log(`copy ` +[...places].sort());

